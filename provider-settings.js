function p(id){return document.getElementById(id)}function base(){return (p('apiBase').value||localStorage.getItem('faleoraApiBase')||'').replace(/\/$/,'')}function saveBase(){if(p('apiBase').value)localStorage.setItem('faleoraApiBase',p('apiBase').value)}async function loadStatus(){saveBase();if(!base())return alert('Backend adresini gir.');try{const r=await fetch(base()+'/api/providers/status');if(!r.ok)throw new Error('HTTP '+r.status);const j=await r.json();p('statusResult').innerHTML='<div class="row"><b>Backend bağlı ✓</b><span>Mod: '+j.notification_mode+'</span></div>';const names={email:'E-posta',sms:'SMS',whatsapp:'WhatsApp',push:'Push'};p('providerCards').innerHTML=Object.keys(names).map(k=>{const x=j[k]||{};return '<div class="row"><b>'+(x.configured?'✓ ':'⚠ ')+names[k]+'</b><span>'+(x.configured?'Yapılandırıldı':'Eksik ayar')+(x.endpoint?' • '+x.endpoint:'')+'</span></div>'}).join('')}catch(e){p('statusResult').innerHTML='<div class="row"><b>Bağlantı kurulamadı</b><span>'+e.message+'</span></div>'}}async function sendTest(){saveBase();if(!base())return alert('Backend adresini gir.');const body={channel:p('testChannel').value,recipient:p('testRecipient').value.trim(),title:p('testTitle').value.trim(),message:p('testMessage').value.trim()};if(!body.recipient)return alert('Test alıcısını gir.');try{const r=await fetch(base()+'/api/providers/test',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error(await r.text());const j=await r.json();p('testResult').innerHTML='<div class="row"><b>'+j.status+'</b><span>'+j.channel+' • '+j.provider+' • '+j.recipient+'</span></div>'}catch(e){p('testResult').innerHTML='<div class="row"><b>Test başarısız</b><span>'+e.message+'</span></div>'}}document.addEventListener('DOMContentLoaded',()=>{p('apiBase').value=localStorage.getItem('faleoraApiBase')||''})